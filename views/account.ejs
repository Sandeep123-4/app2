<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Profile</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600&display=swap');
        
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Inter', sans-serif;
        }

        body {
            background: linear-gradient(135deg, #f5f7fa, #c3cfe2);
            color: #333;
            display: flex;
            flex-direction: column;
            min-height: 100vh;
        }

        .profile-container {
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            padding: 20px;
            gap: 20px;
            background: rgba(255, 255, 255, 0.9);
            border-radius: 10px;
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1);
        }

        .user-profile {
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            margin-bottom: 20px;
        }

        .user-avatar {
            width: 100px;
            height: 100px;
            border-radius: 50%;
            margin-bottom: 10px;
            border: 2px solid rgba(0, 0, 0, 0.05);
        }

        .user-name {
            font-size: 24px;
            font-weight: 600;
            margin-bottom: 10px;
            color: #333;
        }

        .user-handle {
            font-size: 18px;
            font-weight: 400;
            color: #333;
        }

        .tweets-container {
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            gap: 10px;
        }

        .tweet {
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            padding: 10px;
            margin: 10px 0;
            border: 1px solid rgba(0, 0, 0, 0.05);
            border-radius: 5px;
            background: rgba(255, 255, 255, 0.9);
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1);
        }

        .tweet-content {
            font-size: 16px;
            font-weight: 400;
            color: #333;
        }

        .tweet_date {
            font-size: 14px;
            font-weight: 400;
            color: #333;
            margin-top: 5px;
        }

        @media (max-width: 480px) {
            .profile-container {
                padding: 10px;
                gap: 10px;
            }
            
            .user-avatar {
                width: 80px;
                height: 80px;
            }
            
            .user-name {
                font-size: 20px;
            }
            
            .user-handle {
                font-size: 16px;
            }
            
            .tweet {
                padding: 5px;
                margin: 5px 0;
            }
            
            .tweet-content {
                font-size: 14px;
            }
            
            .tweet_date {
                font-size: 12px;
            }
            .profile-container a{
                background-color: #141e30;
            }
    
        }
    </style>
</head>
<body>
    <div class="profile-container">
        <div class="user-profile">
            <img src="/path/to/user/avatar" alt="" class="user-avatar">
            <h2 class="user-name"><%= username %></h2>
            <p class="user-handle"><%= email %></p>
        </div>
        <div class="tweets-container">
            <div class="tweet">
               <a href="/UpdateProfile" class="tweet-content">update</a>
            </div>
        </div>
    </div>
<% if (tweets) { %>
    <% tweets.forEach(tweet => { %>
        <div class="tweet">
            <p class="tweet-content"><%= tweet.tweet %></p>
            <p class="tweet_date"><%= tweet.createdAt.toLocaleDateString() %></p>
        </div>
    <% }); %>
<% } else { %>
    <p>No tweets found.</p>
<% } %>


</body>
</html>